//! STUB: `omajot hub` (docs/PROTOCOL.md §2, §4).
const std = @import("std");

pub fn main(init: std.process.Init, args: []const []const u8) !void {
    _ = init;
    _ = args;
    std.debug.print("omajot hub: not implemented yet\n", .{});
}
